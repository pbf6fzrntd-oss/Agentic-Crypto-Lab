---
description: Handle an inbound lead/demo request via the inbound-demo agent
argument-hint: <lead context, e.g. pasted inbound message or signup detail>
---
Use the inbound-demo agent to qualify and draft a reply for this inbound lead: $ARGUMENTS

Follow the SMB playbook's default inbound flow and log/update the pipeline tracker as instructed in that agent's definition.
