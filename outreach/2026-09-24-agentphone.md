Channel: LinkedIn (first-touch DM)
To: Meet Modi — Co-Founder, AgentPhone (YC P26)
Alternative: Manav Modi — Co-Founder, AgentPhone — send to him instead if Meet is unreachable/inactive, or as a second thread if there's no response.
Touch: 1 of 3 (SMB Playbook default cadence)

> Note before sending: no verified email found for either founder, so this is a LinkedIn DM — verify the account is genuinely Meet's before sending.
>
> Qualification note: AgentPhone is a YC P26, founder-led startup ("phone numbers for AI agents" — provision a number, route inbound/outbound voice and messages through one API, handle A2P registration, spam scoring, threading, and call control). Meet Modi previously built AI agent infrastructure on WhatsApp for 280M+ businesses at Meta — squarely the AI-team-lead/founding-engineer persona, not a platform team at a large org. Early adopters (Google's ADK, Replit, YC, Sim AI, LangChain, Alchemy) confirm they're already selling into agent builders. The signal is unusually direct, not inferred: AgentPhone's entire product is a developer-facing API built specifically for AI agents to call — exposing it as a hosted MCP server so any MCP client (Claude, Cursor, etc.) can invoke it directly is the literal use case. Good fit — proceeding with a signal-based touch 1.

---

Subject: N/A (LinkedIn DM)

Body:

Meet — AgentPhone's API is already built for agents to call directly (numbers, call control, messaging). Today that means each client (Claude, Cursor, whatever an AgentPhone customer is building on) needs its own tool-calling glue to hit it. An MCP server in front of the same API means any MCP client can call AgentPhone's endpoints natively, no custom glue per integration — one more reason for teams like LangChain or Sim AI to standardize on you.

Shredly turns an existing API into a hosted MCP server in minutes — sign in, grab a key, point a client at it. We run the MCP layer, you keep the API.

Worth trying against one AgentPhone endpoint? Free key at shredly.io, no card needed. Happy to loop in Manav instead if this sits better with him.
