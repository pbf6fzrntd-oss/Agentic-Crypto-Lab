Channel: LinkedIn (first-touch DM)
To: Vedant Singh — Co-Founder, Salus (YC W26, San Francisco)
Alternative: Kevin Pan — Co-Founder, Salus — send to whichever founder is more active/reachable on LinkedIn if Vedant doesn't respond.
Touch: 1 of 3 (SMB Playbook default cadence)

> Note before sending: no verified email found for either founder, so this is a LinkedIn DM — verify the account is genuinely Vedant's (or Kevin's) before sending.
>
> Qualification note: Salus is a small, early-stage (YC W26, $4.7M raised) agent-tooling startup founded by two engineers — squarely the "founding/lead AI engineer" persona, not an enterprise platform team. They have a concrete API in scope: their policy-aware proxy/validation layer that intercepts and checks every agent tool call. That's exactly the kind of API worth exposing as a queryable MCP tool in its own right (so a developer can check policy state or why a call was blocked from inside Claude/Cursor, not just via direct SDK integration). Good fit — proceeding with a signal-based touch 1.

---

Subject: N/A (LinkedIn DM)

Body:

Vedant — Salus's proxy sits in the agent's tool-calling path, checking every call against versioned policies and an evidence cache before it executes. That validation/policy layer is itself an API a developer would want to query directly from inside Claude or Cursor — e.g. "why was this call blocked" or "what's the current policy state" — without writing custom SDK glue for it.

Shredly turns that kind of API into a hosted MCP server in minutes — sign in, grab a key, point a client at it. No infra to run on your end.

Worth a quick look? Free key at shredly.io — no card needed to try it.
