Channel: LinkedIn (first-touch DM)
To: Paddy Lambros — Founder, Dex
Touch: 1 of 3 (SMB Playbook cadence)

> ICP fit note: Dex is an early-stage (founded 2025) AI recruiting startup — $8.4M total funding, ~$1.8M ARR, matching 15,000 registered engineers against 50+ tech clients via an LLM-based candidate-profile/matching engine. That's a clear API/data surface (candidate querying and matching) that today is almost certainly only reachable through Dex's own dashboard — exactly the "has data, no MCP server for it yet" signal this motion targets. Company size (small, single-product, seed-stage) fits SMB, not an enterprise platform team.
>
> Persona caveat: Paddy is the founder, but his background is hiring advisory (2.5 years at Atomico), not a technical/AI-team-lead role — he's not a clean match for the "AI-team lead" persona on paper. He's the only named contact and, as founder of a small team, is a reasonable entry point regardless; the message explicitly asks him to route to whoever owns API/integration tooling if that's not him. Drafted as touch 1 with that caveat flagged.
>
> No verified email found for Paddy — this is a LinkedIn DM. Verify the profile/identity before sending.

---

Subject: N/A (LinkedIn DM)

Body:

Paddy — congrats on the round. One thing about Dex that stood out: matching 15k engineers against roles via LLM-based profile analysis means there's a real API under the hood for querying and matching candidates. If that's only reachable through your dashboard today, exposing it as an MCP server would let your client teams (or your own internal tooling) query matches directly from Claude or Cursor — no custom integration glue on either side.

Shredly hosts exactly that: point us at the API, get a key, working MCP endpoint in minutes. Free tier, Google sign-in, no card.

Worth a look — or if API/integrations isn't your call, who on the team should I ask?
