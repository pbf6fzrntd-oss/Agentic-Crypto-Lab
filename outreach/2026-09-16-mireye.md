Channel: LinkedIn (first-touch DM)
To: Ansh Chokshi — Founder, Mireye (YC S26)
Touch: 1 of 3 (SMB Playbook cadence) — see ICP note below before sending

> ICP note: Mireye does not read as a standard Shredly customer. They're not an SMB with an API/dashboard sitting idle for agents — they've already built and shipped their own MCP layer: a hosted remote MCP endpoint (api.mireye.com/mcp, Streamable HTTP + native OAuth) plus a local stdio adapter for clients that don't support that yet (Claude Desktop, Cursor). That's them being agent-infra builders in the same broad space as Shredly, not a prospect with an unexposed API. Same pattern as Monid (see outreach/2026-09-16-monid.md) — drafted as a genuine peer/competitive-intel note, not a sales pitch.
>
> If Ansh replies, the useful outcomes are (a) how self-hosting that dual setup (hosted Streamable HTTP + stdio adapter) is going operationally — uptime, scaling, and keeping client compatibility current as Claude/Cursor/Continue/Windsurf each move at different paces on MCP auth support, and/or (b) whether a second/backup hosting path, or handing off the hosting layer entirely as they scale, is ever worth a look. Don't force either — this is intel/relationship first. No verified email found for Ansh, hence LinkedIn.

---

Subject: N/A (LinkedIn DM)

Body:

Ansh — congrats on the YC S26 launch. Mireye shipping both a hosted remote MCP endpoint (Streamable HTTP + OAuth) and a stdio adapter for clients that don't support that yet is exactly the kind of client-compatibility problem I spend my days on at Shredly, hosting MCP servers so builders don't have to run that layer themselves.

Genuinely curious rather than pitching: how's running both paths yourself going operationally — keeping uptime and auth compatible as Claude/Cursor/Continue/Windsurf each move at their own pace on MCP support? Would be useful to compare notes on where self-hosting an MCP layer gets heavier as usage grows.

No ask beyond a reply if you're up for it — and if a second hosting option (or handing that layer off entirely down the line) is ever worth a look, happy to talk through it then.
