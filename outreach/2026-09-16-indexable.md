Channel: LinkedIn (first-touch DM)
To: Andrew Gazelka — Founder, Indexable (YC S26)
Touch: 1 of 3 (SMB Playbook cadence)

> ICP fit note: Indexable is a 3-person YC S26 team building sandbox infra for agents (full-environment forks/snapshots of files, processes, memory, and databases in ~26ms). That's core agent infrastructure with a clear API surface — provisioning/controlling sandboxes — that developers would want an agent to call directly as a tool, not just something Indexable's own product calls internally. Andrew (ex-xAI, Rust/high-performance systems, prior BountyBot founder) is the AI-team-lead/founding-engineer persona this motion targets. Good fit, drafted as touch 1.
>
> No verified email found for Andrew — this is a LinkedIn DM. Verify the profile/identity before sending.

---

Subject: N/A (LinkedIn DM)

Body:

Andrew — the ~26ms full-environment fork/snapshot approach on Indexable is a neat piece of infra. One thing that stood out: sandbox provisioning and control is itself an API surface a lot of your users will eventually want an agent to call directly ("spin up a sandbox," "snapshot this one," "fork from here") rather than only driving through your SDK.

That's exactly what Shredly hosts — point us at that API and get a working MCP server with a key in minutes, no infra to run. Free tier, Google sign-in, no card.

Worth a look, or happy to just compare notes on where sandbox-as-a-tool-call is headed. Either way, no pressure to hop on a call.
