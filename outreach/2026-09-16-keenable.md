# Keenable — Touch 1 (LinkedIn DM)

**Contact:** Andrey Styskin, Co-founder/CEO (ex-Yandex, led search/AI/cloud division)
**Channel:** LinkedIn DM
**Email:** Not found — verified email unavailable, sending via LinkedIn. **Verify the profile is the correct Andrey Styskin before sending.**

---

## Qualification note (read before sending)

Keenable is on the larger/better-funded end of Shredly's usual self-serve SMB ICP: $26M seed (Accel, Conviction) and ~15 people is a well-resourced, early-growth infra startup, not a scrappy bootstrapped team buying on a card. The headcount (15) technically fits the 1-200 SMB range, and Andrey as a technical co-founder is plausibly the person who'd evaluate this hands-on — so there's a real fit on the persona/signal axis. But the funding profile and the fact that their own product *is* the API being exposed (not an internal tool) means this reads more like a company that would want a quick conversation about a private/dedicated setup than a cold "sign up free" self-serve push. Drafted below with a soft dual CTA (try it or talk) rather than forcing the standard self-serve-only touch-1 CTA — reply/take from here depends on what Andrey signals back.

Genuine signal: Keenable's search index (100B+ documents) is explicitly built API-first for high-frequency agent queries — exactly the shape of thing a dev would want callable as an MCP tool. This isn't a stretch fit; it's a legitimate product tie-in.

---

## Message

Andrey — congrats on the raise. An index built for low-latency, high-frequency agent queries (market mapping, pricing monitoring, lead enrichment) is exactly the kind of API that ends up wrapped in hand-rolled MCP glue code before an agent can actually call it as a tool.

Shredly hosts MCP servers in front of existing APIs — point Claude, Cursor, Continue, or Windsurf at a key, no server code or infra to run on your end. Could be a fast way to expose Keenable's index as a first-class agent tool without your team building and maintaining that layer.

Given where you're at, this might be more of a "what would a private, isolated endpoint look like" conversation than a sign-up-and-try. Happy to do either — send you a key to poke at, or grab 15 minutes?
