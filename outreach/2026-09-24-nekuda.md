Channel: LinkedIn (first-touch DM)
To: Ayal Karmi — Founder, Nekuda (nekuda.ai)
Touch: 1 of 3 (SMB Playbook default cadence)

> Note before sending: no verified email found for Ayal, so this is a LinkedIn DM — verify the account is genuinely his before sending.
>
> Qualification note: Nekuda is a seed-stage fintech startup ($5M seed, Madrona-led, May 2025) building payments infrastructure specifically for AI agents — credential management, authorization, and guardrails so an agent can complete checkout without a human handing over a card. Ayal is the sole named founder, a founding-persona buyer per the ICP. The signal here is direct, not inferred: Nekuda's core product is a payments API built for agents to call, which is the literal use case for exposing it as a hosted MCP server so Claude, Cursor, or any MCP client can invoke it directly instead of going through custom tool-calling glue. Good fit — proceeding with a signal-based touch 1.

---

Subject: N/A (LinkedIn DM)

Body:

Ayal — Nekuda's API is built for agents to call, which means the client side is custom tool-calling glue for every framework that wants to trigger a payment. An MCP server in front of it means Claude, Cursor, or any MCP client calls Nekuda directly with a standard schema, no glue code per integration.

Shredly turns an existing API into a hosted MCP server in minutes — sign in, grab a key, point a client at it. We run the MCP layer; you keep the payments infra.

Worth trying against one endpoint? Free key at shredly.io, no card needed.
