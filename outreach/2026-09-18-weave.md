# Weave — Touch 1 (LinkedIn DM)

**Contact:** Adam Cohen, Co-founder/CEO
**Alternate contact:** Andrew Churchill, Co-founder — try him if Adam doesn't respond
**Channel:** LinkedIn DM
**Email:** Not found — no verified email, sending via LinkedIn. **Verify the profile is the correct Adam Cohen (Weave, weaveos.com) before sending.**

---

## Qualification note (read before sending)

Weave is on the larger/better-funded end of Shredly's usual self-serve SMB ICP: a $13.5M Series A (Standard Capital, YC, Moonfire, Burst Capital, IrregEx, the Agent Fund), 14 employees, and a platform already analyzing 2M+ pull requests across 500+ orgs. That's a company with real traction and headcount past the "one person buying on a card" profile the standard self-serve touch assumes — closer to Keenable's profile than a scrappy two-person shop. Adam is CEO (ops/sales background) rather than the hands-on technical co-founder; Andrew Churchill (ex-Causal) is more likely the one who'd actually evaluate an integration, so he's flagged as the alternate contact.

Genuine signal, not a stretch: Weave Router is itself a drop-in endpoint developers point their coding-agent traffic at (Claude Code, Codex, Cursor) to route requests to the cheapest/best model. That's distinct from routing itself — the router's stats and decisions (which model got picked, cost saved, why) are exactly the kind of thing a developer would want queryable as a tool from inside Claude or Cursor, not just consumed as a dashboard. Given the funding/scale, this reads like a "would a private endpoint make sense" conversation rather than a cold sign-up push — drafted with a dual CTA accordingly.

---

## Message

Adam — congrats on the Series A. Weave Router already sits in front of Claude Code and Codex as an endpoint agents call — separate from that, exposing its routing stats and decisions (which model got picked, cost saved) as a tool an agent can query directly from inside Claude or Cursor is a different surface than a dashboard.

Shredly hosts MCP servers in front of existing APIs — point a client at a key, no server code or infra on your end. Given where Weave's at, might be more useful as a quick call about a private endpoint than a sign-up-and-try. Happy to do either — send a key to poke at, or 15 minutes?
